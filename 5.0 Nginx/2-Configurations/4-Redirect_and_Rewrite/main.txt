n summary, while both URI and URL are used to identify and locate resources on the internet,
URI is the broader term encompassing all resource identifiers, and URL is a specific type of URI that includes information
about how to locate and access a resource. URIs can be further categorized into URLs (locators) and URNs (names).